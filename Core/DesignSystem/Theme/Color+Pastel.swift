import SwiftUI

// Pastel brand colors and elevated surface tokens for the shared design system.
extension Color {
    static let pastelPink = Color(red: 0.98, green: 0.78, blue: 0.82)
    static let sageGreen = Color(red: 0.72, green: 0.84, blue: 0.72)
    static let surfaceElevated = Color(.secondarySystemBackground)
}