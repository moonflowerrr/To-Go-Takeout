import SwiftUI

// Main restaurant feed with category discovery controls.
struct HomeView: View {
    var body: some View {
        NavigationStack { Text("Restaurants").navigationTitle("Home") }
    }
}